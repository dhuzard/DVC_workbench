services:
  dvc-workbench:
    image: ghcr.io/dhuzard/dvc_workbench:latest
    container_name: dvc-workbench
    pull_policy: always
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
