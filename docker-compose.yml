services:
  dvc-workbench:
    build: .
    image: ghcr.io/dhuzard/dvc_workbench:latest
    container_name: dvc-workbench
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
